#include "kmeans.hpp"

kmeans::kmeans(){}
void kmeans::cluster(const MatrixXd &points, MatrixXd &centroids)
{
}
void kmeans::e_step(const MatrixXd &points, MatrixXd &centroids)
{
}
void kmeans::m_step(const MatrixXd &points, MatrixXd &centroids)
{
}
int kmeans::find_nearest_centroid(const RowVectorXd &point, MatrixXd &centroids)
{
    return 0;
}
double kmeans::euclid_distance(const VectorXd& A, const VectorXd& B)
{
    return 0;
}
